spring:
  application:
    name: plateau-backend
  datasource:
    url: jdbc:mysql://localhost:3306/plateau_dev
    username: root
    password: 2360mamun
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:dev@plateau.fr}
    password: ${MAIL_PASSWORD:dev}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
server:
  port: 8080
plateau:
  jwt:
    secret: dev-secret-key-minimum-32-characters!!
    expiry-minutes: 15
  otp:
    expiry-minutes: 10
